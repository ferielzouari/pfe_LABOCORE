server:
  port: 8080
  compression:
    enabled: false

spring:
  application:
    name: labocore

  datasource:
    url: "jdbc:sqlserver://localhost:1433;databaseName=glabo;encrypt=true;trustServerCertificate=true"
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: sa
    password: 123

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: dbo

  h2:
    console:
      enabled: false

  profiles:
    active: local

  mail:
    host: smtp.gmail.com
    port: 587
    username: zouari.feriel12@gmail.com
    password: afen fdvy hbrs ixbe
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# AI Assistant — Gemini API (see ChatServiceImpl)
gemini:
  api-key: ${GEMINI_API_KEY:PUT_YOUR_GEMINI_API_KEY_HERE}
  model: gemini-2.5-flash-lite
  api-url: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-lite:generateContent

logging:
  level:
    com.clinisys.labocore: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.type.descriptor.sql: TRACE